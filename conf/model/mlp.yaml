# Architecture
arch_name: mlp                # "mlp" or "modified_mlp"
input_dim: 2
num_layers: 4
hidden_dim: 64
output_dim: 1
activation: tanh

# Optional: periodic embeddings
# periodicity:
#   period: [2.0]
#   axes: [1]
#   trainable: false

# Optional: Fourier feature embeddings
# fourier_emb:
#   embed_scale: 1.0
#   embed_dim: 128

# Optional: weight factorization
# reparam:
#   type: weight_fact
#   mean: 1.0
#   stddev: 0.1
