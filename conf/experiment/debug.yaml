# @package _global_
hydra:
  run:
    dir: outputs/debug/${now:%Y-%m-%d}/${now:%H-%M-%S}

model:
  hidden_dim: 2
  num_layers: 2

optimizer:
  epochs: 10
  lbfgs:
    use: True

trainer:
  max_steps: 2
  batch_size:
    domain: 100
    boundary: 10
    initial: 10
  rad:
    use: true
    ratio: 0.8
    batch:
      domain: 1000
      boundary: 2
      initial: 2

wandb:
  project: "tDWfPINN-debug-eg1"
  entity: null
  mode: "online" # Default to disabled for dev, user can enable
  group: null
